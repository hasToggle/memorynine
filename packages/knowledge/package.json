{
  "name": "@repo/knowledge",
  "version": "0.0.0",
  "private": true,
  "main": "./index.ts",
  "types": "./index.ts",
  "scripts": {
    "clean": "git clean -xdf .cache .turbo dist node_modules",
    "eval-extraction": "bun scripts/eval-extraction.ts",
    "probe-zdr": "bun scripts/probe-zdr.ts",
    "seed-evals": "bun scripts/seed-evals.ts",
    "setup-indexes": "bun scripts/setup-indexes.ts",
    "test": "bun test",
    "typecheck": "tsc --noEmit --emitDeclarationOnly false"
  },
  "dependencies": {
    "@t3-oss/env-nextjs": "^0.13.11",
    "mongodb": "^7.5.0",
    "zod": "^4.4.3"
  },
  "devDependencies": {
    "@repo/typescript-config": "workspace:*",
    "@types/node": "26.1.2",
    "typescript": "^7.0.2"
  }
}
